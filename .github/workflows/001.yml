name: CALL OPENFISCALDATA
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Hello, world!
      - name: Call Data
        env: 
          API_KEY: ${{secrets.PENFISCALDATA_OPENAPI_KEY}}
        run: |
          echo CALL OPENFISCALDATA
